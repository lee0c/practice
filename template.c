#include <stdio.h>
#include <stdlib.h>

#include "debug.h"

/* Problem statement: */

/* Compile with: 
gcc -Wall name.c -o name.o

Toggle debug mode in debug.h
*/

int main (int argc, char* argv[])
{
  if (argc != /* val */)
  {
    printf("Usage: ");
    return EXIT_FAILURE;
  }

  return EXIT_SUCCESS;
}
